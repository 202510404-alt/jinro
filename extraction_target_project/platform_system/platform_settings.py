# platform_system/platform_settings.py

# 🧱 플랫폼 기본 규격 설정
DEFAULT_PLATFORM_WIDTH = 200    # 발판 기본 가로 길이
DEFAULT_PLATFORM_HEIGHT = 30    # 발판 기본 세로 두께 (높이)

# ⚙️ 플랫폼 기본 기믹 설정 (디폴트 값)
DEFAULT_IS_SOLID = True                # 기본적으로 밟을 수 있는 단단한 벽인가?
DEFAULT_IS_VISIBLE = True              # 기본적으로 눈에 보이는가?
DEFAULT_PASSABLE_FROM_BOTTOM = False   # 기본적으로 아래에서 위로 뚫고 올라갈 수 있는가?

# 🚀 움직이는 발판 등 미래 기믹용 기본 속도 설정
DEFAULT_PLATFORM_SPEED_X = 2
DEFAULT_PLATFORM_SPEED_Y = 2

